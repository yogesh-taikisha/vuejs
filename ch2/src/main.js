// entry point 

// imports
import './assets/main.css'
import { createApp } from 'vue'
import App from './App.vue'

// create app is function which takes component App as the input and mount method is used  and takes the id #app
createApp(App).mount('#app')
